syntax = "proto3";

enum ProxyHeaderKey {
    REMOTE_ADDRESS = 0; // Remote address to connect. e.g. 8.8.8.8:53
}

service ProxyService {
  rpc Proxy(stream Bytes) returns (stream Bytes) {}
}

message Bytes {
  bytes data = 1;
}

